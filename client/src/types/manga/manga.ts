export type manga = {
  id: number;
  name: string;
  description: string;
  title: string;
  episodesNum: number;
};

export type image = {
  imagesNum: number;
  episodeNum: number;
  mangaID: number;
};
